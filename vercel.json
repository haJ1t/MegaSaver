{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "echo 'static marketing site — no dependencies to install'",
  "buildCommand": "ROOT=$(git rev-parse --show-toplevel 2>/dev/null || pwd); rm -rf .out && mkdir -p .out && tar -C \"$ROOT/site\" --exclude=.out -cf - . | tar -C .out -xf -",
  "outputDirectory": ".out",
  "cleanUrls": true,
  "trailingSlash": false
}
