---
"@megasaver/output-filter": minor
---

Add the command-filter registry: ten structured command filters (git-status, git-log, docker-ps, docker-build, kubectl-get, gh-pr-list, npm-install, pip-install, cargo-build, terraform-plan) behind the W4 reconstruct-or-declare integrity gate. A registry hit preempts category compressors in the compressed band, skips simhash dedupe, and folds noise behind counted `… [N …]` evidence markers. New exports: `COMMAND_FILTERS`, `COMMAND_FILTER_MARKERS`, `matchCommandFilter`, `CommandFilter`, `CommandFilterIntegrity`; `CompressorName` grows append-only with the ten filter names. The next 20 filters are mechanical via `packages/output-filter/COMMAND-FILTERS.md`.
