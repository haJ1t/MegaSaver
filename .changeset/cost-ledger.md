---
"@megasaver/stats": minor
"@megasaver/core": minor
"@megasaver/cli": minor
---

Add the unified cost ledger: pure `buildCostLedger` rollup in stats, core re-export, and the read-only `mega cost` command (receipts only, explicit UNKNOWN bucket, measured savings pairs only, tokens not dollars).
